/*

const objID = apiData.filter(function (data) {
  return data.id >= 0 && data.price >= 0;
});
const mandatoryKeys = objID.map((data) => {
  return;
  {
    id: data.id;
    price: data.price;
  }
});

/*Sackgasse*/

console.log(mandatoryKeys);
*/

function filterApiData(apiData, mandatoryKeys) {}
